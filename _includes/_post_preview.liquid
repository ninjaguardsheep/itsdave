<div class="post_preview">
    <h3>{{ post.title }}</h3>
    <p>{{ post.published_date | date: "%b %d, %Y" }} | {% include _reading_time.liquid %}</p>
    <p>{{ post.description }}</p>
    <a href="{{post.permalink}}">Read more →</a>
</div>
